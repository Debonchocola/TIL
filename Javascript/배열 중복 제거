const set = new Set([...preventDupl, [j, column.id]].map(item => JSON.stringify(item))); // set에 담고 하나씩 stringify를 통해 중복을 제거한다.

const result = Array.from(set).map(item => JSON.parse(item)); //다시 배열에 담아준다.
