function solution(my_string, s, e) {
    var answer = '';
    let old = String(my_string.slice(s,e+1));
    let newString = String(my_string.slice(s,e+1)).split("").reverse().join("");
    
    return my_string.replace(old, newString);
}
