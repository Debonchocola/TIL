function solution(arr)
{
    var answer = [];
    for(let i = 0; i< arr.length; i++) {
        if(arr[i] !== arr[i+1]) {       //중복되지 않을 때만 push하도록 했다.
            answer.push(arr[i])
        }
    }
    return answer;
}
